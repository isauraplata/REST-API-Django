from django.db import models

# Create your models here.

# Customizing Django's default user model to include additional permissions.
# This allows for managing permissions beyond the standard user authentication.
# Ensure to document the changes made to the default user model for clarity.
